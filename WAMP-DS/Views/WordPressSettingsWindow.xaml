<Window x:Class="WAMP_DS.Views.WordPressSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WordPress Settings"
        Height="515"
        Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E"
        Foreground="White">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#666666"/>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel>
            <TextBlock Text="WordPress Installation"
                   FontSize="24"
                   FontWeight="Bold"/>

            <TextBlock Text="Configure your WordPress site and administrator account"
                   Margin="0,5,0,20"
                   Opacity="0.7"/>
        </StackPanel>

        <!-- Main Settings -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Site Title"
                   Margin="0,0,0,5"/>

            <TextBox x:Name="SiteTitleTextBox"
                 Height="30"
                 Margin="0,0,0,15"/>

            <TextBlock Text="Admin Username"
                   Margin="0,0,0,5"/>

            <TextBox x:Name="AdminUsernameTextBox"
                 Height="30"
                 Margin="0,0,0,15"/>

            <TextBlock Text="Admin Password"
                   Margin="0,0,0,5"/>

            <PasswordBox x:Name="AdminPasswordBox"
                     Height="30"
                     Margin="0,0,0,15"/>

            <TextBlock Text="Admin Email"
                   Margin="0,0,0,5"/>

            <TextBox x:Name="AdminEmailTextBox"
                 Height="30"
                 Margin="0,0,0,15"/>

            <CheckBox x:Name="DiscourageSearchEnginesCheckBox"
                  Content="Discourage search engines from indexing this site"
                  Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,30,0,0">

            <Button Content="Cancel"
                    Width="120"
                    Height="35"
                    Margin="0,0,15,0"
                    Padding="10,5"
                    Click="CancelButton_Click"/>

            <Button Content="Continue"
                    Width="120"
                    Height="35"
                    Padding="10,5"
                    Click="ContinueButton_Click"/>
        </StackPanel>
    </Grid>
 
</Window>