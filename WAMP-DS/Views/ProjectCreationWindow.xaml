<Window x:Class="WAMP_DS.Views.ProjectCreationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Creating Project"
        Width="650"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>


        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Grid.ColumnSpan="2"
                   Text="Creating Project"
                   Foreground="#CCCCCC"
                   FontSize="22"
                   FontWeight="SemiBold"/>


        <!-- Status -->
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   x:Name="StatusText"
                   Text="Building Your Laravel Project..."
                   Margin="0,10,0,0"
                   VerticalAlignment="Center"
                   Foreground="#CCCCCC"
                   FontSize="14"/>


        <!-- Buttons -->
        <StackPanel Grid.Row="1"
                    Grid.Column="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="10,10,0,0">


            <Button x:Name="DetailsButton"
                    Content="Show Details"
                    Width="120"
                    Height="32"
                    Margin="0,0,10,0"
                    Click="DetailsButton_Click"/>


            <Button x:Name="CloseButton"
                    Content="Close"
                    Width="100"
                    Height="32"
                    Visibility="Collapsed"
                    Click="CloseButton_Click"/>


        </StackPanel>


        <!-- Progress -->
        <ProgressBar Grid.Row="2"
                     Grid.ColumnSpan="2"
                     x:Name="ProgressBar"
                     Height="20"
                     Margin="0,20,0,0"
                     IsIndeterminate="True"/>


        <!-- Details -->
        <TextBox Grid.Row="3"
                 Grid.ColumnSpan="2"
                 x:Name="DetailsTextBox"
                 Visibility="Collapsed"
                 Margin="0,10,0,0"
                 Background="#111111"
                 Foreground="#CCCCCC"
                 BorderBrush="#333333"
                 FontFamily="Consolas"
                 FontSize="12"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>


    </Grid>

</Window>